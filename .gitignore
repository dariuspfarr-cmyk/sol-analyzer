# ── Python ────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.ruff_cache/

# ── Node / ESLint ─────────────────────────────────────────
node_modules/
npm-debug.log*

# ── Laufzeit-Daten des Bots (ändern sich ständig) ─────────
*.db
*.db-journal
*.db-wal
*.db-shm
state.json
trades.csv
trades.json
daily_performance.json
error.log
*.log
*.jsonl
api_costs.jsonl
routing_log.jsonl

# ── Caches ────────────────────────────────────────────────
research_cache.json
bull_run_cache.json
poi_log.json
poi_stats.json

# ── Auto-generierter Verbesserungs-Backlog (Laufzeit-Artefakt) ──
# Der Mechanismus (improvement_scanner.py, CLAUDE.md) ist versioniert;
# die generierten Backlog-Dateien nicht (ändern sich bei jedem Scan).
IMPROVEMENTS.md
improvements.json

# ── Generierte Charts ─────────────────────────────────────
charts/
*.png

# ── Secrets / lokale Umgebung ─────────────────────────────
# .env kann künftig Secrets enthalten → nie committen.
# Die Vorlage .env.example bleibt versioniert.
.env

# ── OS / Editor ───────────────────────────────────────────
Thumbs.db
.DS_Store
.vscode/*.local
