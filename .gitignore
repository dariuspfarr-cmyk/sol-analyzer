# Secrets
.env

# Datenbank
*.db
*.db.old
*.sqlite
*.sqlite3

# Lerndaten / generierte Dateien (ändern sich bei jedem Lauf)
*.pkl
active_strategy.json
backtest_weights.json
config_backup.json
model_report.json
performance_report.json
rule_performance.json
strategy_evolution.json
strategy_rules.json
strategy_selection.json
routing_report.json

# Python
venv/
__pycache__/
*.pyc

# Charts/Output
*.png

# npm
node_modules/

# Logs und generierte Daten
*.log
*.jsonl
poi_log.json
research_cache.json
state.json
strategy_params.json
trades.csv
trades.json

# Live-Trading Laufzeit-/Sicherheitsdateien (nie committen)
live_orders.log
live_daily.json
LIVE_KILL
live_positions.json
trend_state.json

# Laufzeit-mutierte Dateien — werden vom Bot bei jedem Lauf neu geschrieben und
# sollen NICHT getrackt werden (sonst kollidiert jeder Deploy-Pull mit ihnen).
# config.json wird vom threshold_optimizer angepasst; config.py faellt auf
# BOUNDS-Defaults zurueck, wenn die Datei fehlt (fresh clone bleibt lauffaehig).
config.json
improvements.json
IMPROVEMENTS.md
bull_run_cache.json
daily_performance.json
.last_backtest_live_count
.last_backtest_run
.last_daily_summary
.last_weekly_run
